import type { Config } from "tailwindcss";

const config: Config = {
  theme: {
    extend: {
      colors: {
        "brand-mint": "#E6F6F2",
        "brand-teal": "#14B8A6",
        "brand-ink": "#0F172A",
        "brand-muted": "#64748B",
        "brand-line": "#E2E8F0",
      },
    },
  },
};

export default config;
